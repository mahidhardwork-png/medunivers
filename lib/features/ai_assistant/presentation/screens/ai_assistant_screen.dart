import 'package:flutter/material.dart';
import '../../../../shared/widgets/ui_kit.dart';
class AiAssistantScreen extends StatelessWidget { const AiAssistantScreen({super.key}); @override Widget build(BuildContext context)=> const Scaffold(body: SafeArea(child: Padding(padding: EdgeInsets.all(16), child: InfoCard(child: Text('AI clinical co-pilot with triage, guidance, and future multimodal inference hooks.'))))); }
